# supabase/

- `migrations/` → SQL migration dosyaları (şema, RLS policy'leri)
- `functions/`  → Supabase Edge Functions (bar decay, streak, bildirim tetikleme)

Bu klasör G1 (Data + Logic) görevleri kapsamında doldurulacak.
